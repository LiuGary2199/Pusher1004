using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Outright : MonoBehaviour
{
[UnityEngine.Serialization.FormerlySerializedAs("FX_Light")]    public GameObject FX_Flora;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
